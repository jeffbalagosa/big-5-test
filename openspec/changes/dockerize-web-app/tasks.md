# Tasks: Dockerize Web App

- [x] 1.1 Create `.dockerignore` to exclude `node_modules`, `dist`, `.venv`, and other unnecessary files.
- [x] 1.2 Create `nginx.conf` to handle SPA routing.
- [x] 1.3 Create `Dockerfile` with multi-stage build (Node.js build -> Nginx serve).
- [x] 1.4 Create `docker-compose.yml` for easy local execution.
- [x] 1.5 Update `README.md` with Docker instructions.
- [x] 1.6 Verify the Docker build and run process locally.
